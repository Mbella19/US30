# Hybrid US30 Trading System Dependencies
# Optimized for Apple M2 Silicon (8GB RAM)

# Core Deep Learning
torch>=2.0.0

# Reinforcement Learning
gymnasium>=0.29.0
stable-baselines3>=2.1.0
sb3-contrib>=2.2.0  # RecurrentPPO (LSTM) - EXPERIMENTAL

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0  # Required for pandas read_parquet/to_parquet (pipeline + live bridge)

# Technical Analysis (optional - we implement our own indicators)
# pandas-ta>=0.3.14b  # Commented out due to compatibility issues

# Machine Learning Utilities
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.13.0  # Used by backtest/visualization scripts
mplfinance>=0.12.10b0  # Candlestick/trade visualizations

# Progress Bars
tqdm>=4.65.0

# System utilities
psutil>=5.9.0

# Optional: For faster technical indicators (requires system installation)
# TA-Lib>=0.4.28
websockets>=12.0

# Testing (optional but recommended)
pytest>=7.0.0
